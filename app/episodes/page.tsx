import s from "@/app/feature-page.module.scss";

export default function EpisodesPage() {
    return (
        <main className={s.page}>
            <section className={s.card}>
                <h1 className={s.title}>Episodes</h1>
                <p className={s.subtitle}>Episodes Component</p>
            </section>
        </main>
    );
}

